fastapi==0.115.0
uvicorn[standard]==0.32.0
websockets==13.1
openwakeword==0.5.0
numpy==2.1.0
python-multipart==0.0.12
# Note: tflite-runtime is not included - we use ONNX only (inference_framework='onnx')
# Install with: pip install --no-deps openwakeword==0.5.0 (after installing other deps)

fastapi==0.115.0
uvicorn[standard]==0.32.0
websockets==13.1
numpy==2.1.0
python-multipart==0.0.12
onnxruntime>=1.10.0,<2.0.0
scipy<2.0,>=1.3
scikit-learn<2.0,>=1.0
# Note: tflite-runtime is not included - we use ONNX only (inference_framework='onnx')
# Install openwakeword with --no-deps to avoid tflite-runtime:
# pip install --no-deps openwakeword==0.5.0
openwakeword==0.5.0
